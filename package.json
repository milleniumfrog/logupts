{
  "name": "logupts",
  "version": "3.0.5",
  "description": "LogUpTs is an extendable class, which logs your message with a generated prefix and postfix. The prefix and postfix strings have the possibility to add Placeholder that will be replaced when the script executes",
  "main": "./dist/logupts.js",
  "types": "./dist/logupts.d.ts",
  "scripts": {
    "test": "mocha -r node_modules/ts-node/register src/**/*.spec.ts",
    "watch": "tsc --watch & rollup -c --watch",
    "karma": "karma start",
    "build": "tsc -p tsconfig.es2015.json && tsc -p tsconfig.json && rollup -c && uglifyjs dist/browser/logupts.js -o dist/browser/logupts.min.js",
    "externals": "npm update strplace && cp node_modules/strplace/src/strplace.ts src/external/strplace.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/milleniumfrog/logupts.git"
  },
  "author": "René Schwarzinger",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/milleniumfrog/logupts/issues"
  },
  "homepage": "https://github.com/milleniumfrog/logupts#readme",
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.5",
    "chai": "^4.1.2",
    "karma": "^2.0.5",
    "karma-chrome-launcher": "^2.2.0",
    "karma-mocha": "^1.3.0",
    "karma-typescript": "^3.0.13",
    "mocha": "^5.2.0",
    "rollup": "^0.63.5",
    "rollup-plugin-commonjs": "^9.1.4",
    "rollup-plugin-node-resolve": "^3.3.0",
    "strplace": "0.0.4",
    "ts-node": "^7.0.0",
    "typescript": "^2.9.2",
    "uglify-es": "^3.3.9"
  },
  "dependencies": {}
}
